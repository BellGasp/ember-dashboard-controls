import Component from '@ember/component';
import layout from '../../../templates/components/base/db-iconav/iconav-items';

export default Component.extend({
  layout,

  classNames: 'iconav-slider'
});
