import Ember from 'ember';
import layout from '../../../templates/components/base/db-navbar/navbar-items';

export default Ember.Component.extend({
  layout,

  classNames: 'db-navbar-items collapse navbar-collapse mr-auto'
});
