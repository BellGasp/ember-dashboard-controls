import Ember from 'ember';
import layout from '../../../templates/components/base/db-navbar/navbar-brand';

export default Ember.Component.extend({
  layout,

  tagName: '',
  destination: 'index'
});
