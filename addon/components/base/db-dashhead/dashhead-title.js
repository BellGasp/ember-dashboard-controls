import Component from '@ember/component';
import layout from '../../../templates/components/base/db-dashhead/dashhead-title';

export default Component.extend({
  layout,

  tagName: 'h3',
  classNames: 'dashhead-title'
});
