import Ember from 'ember';
import layout from '../../../templates/components/base/db-dashhead/dashhead-titles';

export default Ember.Component.extend({
  layout,

  classNames: 'dashhead-titles'
});
