import Component from '@ember/component';
import layout from '../../../templates/components/base/db-dashhead/dashhead-subtitle';

export default Component.extend({
  layout,

  tagName: 'h6',
  classNames: 'dashhead-subtitle'
});
