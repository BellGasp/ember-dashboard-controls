import Ember from 'ember';
import layout from '../../../templates/components/base/db-dashhead/dashhead-toolbar';

export default Ember.Component.extend({
  layout,

  classNames: 'dashhead-toolbar'
});
